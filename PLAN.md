# Oracle Page — 개발 계획

## 현재 상태 (완료)

### 1단계
- [x] 커뮤니티 피드 (OracleCard, BettingButtons, QuickBetStrip)
- [x] 추천/인기 패널 (RecommendationPanel, TrendingBadge)
- [x] 포켓몬 7단계 등급 시스템 (잉어킹→아르세우스)
- [x] 관리자 페이지 (/admin): 대시보드, 사용자관리, 예언관리, 등급설정

### 2단계 (Phase 1~5)
- [x] Phase 1 — 예언 생성 플로우 (`CreateOracleModal`, FAB 연결)
- [x] Phase 2 — 예언 상세 페이지 (`app/oracle/[id]`, 배팅 차트, 댓글 섹션)
- [x] Phase 3 — 유저 프로필 페이지 (`app/profile/[id]`, 등급 카드, 배팅 내역)
- [x] Phase 4 — 알림 시스템 (`NotificationPanel`, 헤더 Bell)
- [x] Phase 5 — 예언 결과/정산 UI (`OracleResult`)

UI/화면 단위로는 전부 구현되어 있었으나, 상당수가 실제 데이터·로직과 끝까지 연결되어 있지 않았음. 3단계에서 이를 메움.

---

## 3단계 — 기능 완성 & 안정화 ✅ 완료

Phase A~D, E-1 구현 완료. `tsc` / `lint` / `build` 통과, Playwright 로 실제 브라우저 동작까지 검증함.

### 구현 중 발견해 고친 버그

계획 수립 시점에는 몰랐던, 실제로 앱을 띄워서야 드러난 문제들:

- **예언 상세 / 프로필 페이지가 런타임에서 전부 500** — `use(params)` 는 Next 15 API 인데 이 프로젝트는 Next 14. 빌드는 통과하지만 페이지가 열리지 않는 상태였음
- **상세 페이지에서 배팅이 두 번 실행** — `BettingButtons` 가 `placeBet` 호출 후 `onBet` 도 호출하고, 상세 페이지의 `onBet` 이 다시 `placeBet` 을 부름
- **관리자 결과 미리보기가 엉뚱한 정답 표시** — 관리자가 고른 옵션이 아니라 "최다 득표 옵션"을 정답으로 표시
- **QuickBetStrip 이 가짜 배팅** — 로컬 state 만 바꾸고 실제 포인트 차감/기록은 안 함
- **생성한 예언이 홈에서 안 보임** — 홈은 `isHot` 만 노출하는데 새 예언은 `isNew` 라 어디에도 안 뜸 → 홈에 "새로 올라온 예언" 섹션 추가로 해소

### Phase A — 긴급 버그 수정 ✅

- `app/page.tsx` 의 잘못된 타입 문법(`typeof import(...).Oracle[]`)을 고쳐 프로덕션 빌드 복구
- `placeBet` 이 예언 통계를 반영하도록 수정: 선택 옵션 `totalBets` 증가, 전체 옵션 선택률/배당 재계산, `totalPool` 누적, 신규 참여자일 때만 `totalParticipants` +1

### Phase B — 배팅 → 정산 → 포인트 루프 ✅

- `Oracle.winningOptionId` 추가 — 종료된 예언의 정답을 저장할 자리가 생김
- `closeOracle` 이 정답 저장 + 당첨금 계산 + **자동 지급** + 적중률/적중 수 갱신 + 결과 알림까지 처리
- `MyBetRecord.odds` 추가 — **배당률을 배팅 시점에 고정**. 이후 시장 배당이 변해도 정산은 배팅 당시 배당으로 이뤄짐
- 상세 페이지에서 종료된 예언에 `<OracleResult>` 렌더 (원래 유저용인데 관리자 화면에서만 쓰이고 있었음)
- 이미 정산된 배팅은 재정산에서 제외 — 예언을 다시 열었다 닫아도 이중 지급되지 않음

> 정산 방식은 **자동 지급**으로 확정. 수동 "수령하기" 버튼은 유저가 방문하지 않으면 영영 못 받는 구조라 제거함.

### Phase C — 관리자 ↔ 실데이터 연동 ✅

- 유저 로스터를 context 로 승격 (`users`, `me` 는 여기서 파생) — 관리자 편집이 로그인한 "나" 에게 실제 반영됨
- 등급 임계값을 동적화 (`resolveGrades`, `useGrades`) — 등급설정 저장 시 전 유저 등급이 즉시 재계산됨
- 등급을 수동 지정한 유저(`gradeOverride`)는 자동 재계산에서 제외
- 대시보드 등급 분포를 실제 유저 데이터 기준으로 계산 (기존의 임의 ×3 스케일링 제거)
- 등급별 일일 생성 한도 실제 적용. 규칙을 `lib/grades.ts` 의 `dailyOracleLimit()` 한 곳으로 통합하고, 어긋나 있던 혜택 문구도 맞춤

### Phase D — 마무리 기능 ✅

- `components/FollowButton.tsx` 신규 — 내 프로필이 아닐 때만 노출
- 댓글을 context 로 승격해 예언별로 분리. 기존엔 모든 예언에 동일한 seed 댓글 3개가 떴고 `MOCK_COMMENTS` 는 죽은 코드였음
- 리더보드에 "나" 포함, 상위권 밖이면 내 순위를 하단에 따로 노출
- 홈에 "새로 올라온 예언" 섹션 추가

### Phase E-1 — localStorage 영속성 ✅

- `lib/storage.ts` 신규 — Date 복원(ISO 문자열 → Date)을 포함한 저장/로드 헬퍼
- 배팅 / 생성한 예언 / 알림 / 포인트 / 댓글 / 팔로우 / 등급 임계값이 새로고침 후에도 유지됨
- SSR 하이드레이션 미스매치를 피하려 **마운트 이후에만** 저장값을 적용

### 검증 방법

`tsc --noEmit`, `next lint`, `next build` 통과. 추가로 Playwright 로 실제 브라우저에서 확인:

- 배팅 → 참여자/풀 증가 → 새로고침 후 유지 → 관리자 정산 → 포인트 자동 지급
  (1,500P 에서 50P 배팅 후 배당 x1.4 로 70P 지급 → 1,520P, 계산 일치)
- 관리자에서 포인트 변경 시 헤더 등급이 실제로 변경됨
- 이상해씨 등급에서 예언 3개 생성 후 4번째 차단
- 댓글 작성, 팔로우 토글, 결과 카드 노출

---

## 4단계 — 오락성 & 사용성 ✅ 완료

진단: **유저가 배팅한 결과를 영원히 볼 수 없었다.** `endsAt` 은 표시·정렬에만 쓰였고 마감이 지나도 `status` 는 `"live"` 로 남아, 관리자가 손으로 닫기 전엔 아무 일도 일어나지 않았다. 포인트를 버는 길도 적중뿐이라 연패하면 0P 에서 게임이 끝났다.

### A — 루프 닫기
- **A1** 마감 시각 자동 정산. 선택률 가중 랜덤으로 결과를 확정하고, 오래 비웠다 돌아와도 밀린 예언을 따라잡는다. (실서비스에선 이 자리가 관리자 승인 큐로 대체되어야 함)
- **A2** 초단기 예언(10분·30분·1시간) + 홈 "곧 결과" 카운트다운 스트립. 예언 생성 마감 기간도 분 단위(5분~30일)로 전환
- **A3** 등급별 일일 출석 보너스 + 포인트 고갈 시 재기 지원금
- **A4** 첫 방문자용 90초 연습 예언과 온보딩 카드

### B — 눌러도 반응 없던 것들
- **B1** 빠른 배팅이 첫 선택지에만 걸리던 문제 → 모든 선택지에 원터치 제공
- **B2** 종료된 예언 카드가 배팅 UI 를 그리던 문제 → 결과 요약으로 교체. 마감 지난 예언의 배팅도 차단
- **B3** 알림에 `oracleId` 를 저장해두고 쓰지 않던 문제 → 해당 예언으로 이동
- **B4** 태그에 커서만 있고 동작이 없던 문제 → 태그 필터
- **B5** 배팅 취소 불가 → 마감 전 취소 + 포인트·통계 원복
- **B6** 내 예언 탭 신설, 커뮤니티에 "참여한 예언만" 필터

### C — 재방문 동기
- **C1** 연승 스트릭. 3/5/7연승 구간별 배당 보너스, 프로필·리더보드 노출
- **C2** 죽어 있던 `accuracyBonus` 필드를 등급별 배당 보너스로 연결
- **C3** 등급 혜택 문구를 실제 규칙에서 생성(`perksFor`) — 구현과 어긋날 수 없게 함. 구현되지 않은 약속(VIP 채널, 투표 가중치, 포인트 N배, 홈 피처드)은 제거
- **C4** 승급 시 전체 화면 진화 연출 (`prefers-reduced-motion` 존중)
- **C5** 실시간 배팅 티커 + 팔로잉 피드
- **C6** 적중 결과를 캔버스 카드로 만들어 공유·저장

### 부수 수정
- 렌더 중 `Date.now()` 사용으로 발생하던 **하이드레이션 오류(React #425) 제거**. 3단계에서 "정리 대상"으로 남겨뒀던 항목인데 실제로 콘솔 에러를 내고 있었다. 시간 의존 표시를 `useNow` 훅으로 모아 마운트 이후에만 계산한다.
- 배당은 배팅 시점 값으로 고정되므로, 등급·연승 보너스도 그 시점 값을 기록해 정산한다.

### 검증
Playwright 로 브라우저에서 확인 — 자동 정산(90초짜리 연습 예언이 실제로 90초 뒤 정산됨), 보너스 수령(1,500P → 1,850P), 두 번째 선택지 원터치 배팅, 배팅 취소 후 UI 복귀, 태그 필터, 알림 이동, 승급 연출(990P → 보너스 → 이상해씨 진화 오버레이), 배당 보너스 계산(100P × 1.55 × 1.05 = 162P), 팔로잉 필터. **런타임 에러 0.**

---

## 5단계 — 안정화 ✅ 완료

### 유닛 테스트 도입 (43종)

포인트가 오가는 계산이 React 컴포넌트 안에 있어 검증이 불가능했다. 순수 함수로 분리하고 테스트를 붙였다.

- `lib/settlement.ts` 신규 — `payoutFor` / `profitFor` / `settleBets`. 정산 로직을 context 에서 떼어냈다
- 테스트: 정산 10, 배당 12, 등급 21
- **혜택 문구가 실제 규칙과 일치하는지**, **구현되지 않은 약속이 남아 있지 않은지**도 테스트로 고정 — 4단계에서 정리한 내용이 다시 어긋나지 않도록
- 러너는 Node 22 내장 `node:test`. **추가 의존성 없음** (`npm test`)

### Next 16 + React 19 업그레이드 — 취약점 0건

Next 14 는 패치로 막을 수 없는 advisory 가 13건 남아 있었다(16.3.3 에서 수정). 14.x 에 머무는 대신 메이저를 올렸다.

- `next` 14.2.35 → 16.3.3, `react`/`react-dom` 18 → 19
- `lucide-react` 0.378 → 1.34 (React 19 peer 충족)
- `params` 가 다시 Promise 이므로 `use(params)` 로 해석
- `next lint` 가 제거되어 ESLint flat config 로 전환
- **`npm audit` 0건**

### React Compiler 린트가 잡아낸 문제 4건

Next 16 의 새 린트 규칙이 기존 코드의 실제 문제를 드러냈다.

- `useNow` 를 `useSyncExternalStore` 로 재작성. 타이머 하나를 공유하고 각자 원하는 정밀도로 값을 내림해, 값이 그대로면 리렌더를 건너뛴다
- 홈 탭 전환을 effect 대신 `tagFilter` 에서 파생
- `RecommendationPanel` / `CreateOracleModal` 이 렌더 중 `Date.now()` 를 부르던 부분 제거
- localStorage 하이드레이션은 effect 가 불가피해 이유를 적고 그 부분만 예외 처리

### 검증

`lint` / `tsc` / `test`(43) / `build` 통과. 브라우저에서 16개 기능 전체 재확인 — 자동 정산(90초), 보너스 계산(100P × 1.55 × 1.05 = 162P), 승급 연출, 배팅 취소, 태그 필터, 알림 이동, 팔로잉 필터. 하이드레이션·런타임 에러 0.

---

## 6단계 — Supabase 백엔드 ✅ 구현 완료 (서버 검증 대기)

E-2 를 Supabase 로 진행. 환경변수가 없으면 기존 localStorage 모드로 동작하므로, 계정 없이도 그대로 실행된다.

### 가장 중요한 변화 — 포인트 계산이 서버로

로컬 모드는 클라이언트가 곧 서버라 포인트 계산도 브라우저에서 했다. 실제 DB 를 붙이는 순간 그 구조는 **REST API 로 누구나 자기 포인트를 고칠 수 있다**는 뜻이 된다. 그래서 신뢰 경계를 옮겼다.

- RLS 로 `profiles` 의 포인트·전적·권한 직접 수정을 막고, 트리거로 한 번 더 차단
- 배팅·취소·정산·보너스·예언생성은 전부 `SECURITY DEFINER` 함수를 통해서만
- 남의 배팅 내역은 보이지 않고, 티커에 필요한 최소 정보만 뷰로 노출

### `supabase/migrations`

| 파일 | 내용 |
| --- | --- |
| `..._schema.sql` | 테이블·인덱스, 가입 시 프로필 자동 생성, 한 예언 한 번 배팅을 unique 제약으로 강제 |
| `..._rls.sql` | RLS 정책, 포인트 직접 수정 차단 트리거, 댓글 수·좋아요 동기화 |
| `..._functions.sql` | `place_bet` / `cancel_bet` / `settle_oracle` / `claim_daily_bonus` / `claim_relief` / `create_oracle` / `admin_*` |
| `..._cron.sql` | `settle_due_oracles()` 를 매분 실행 — **브라우저를 닫아도 정산된다** |

등급·배당·연승 규칙은 SQL 과 TS 양쪽에 같은 값으로 구현되어 있다. 규칙을 바꿀 때는 **둘 다** 고쳐야 한다.

### 앱

- `lib/data` — `DataSource` 인터페이스와 `local` / `supabase` 두 구현. context 를 그 위로 옮겨 상태기계는 하나로 유지
- 정산·배당 계산은 기존 순수 함수(테스트 43종)를 양쪽이 공유
- 로그인·회원가입 페이지, 세션 갱신 미들웨어
- 관리자 게이트를 평문 비밀번호에서 프로필 `role` 로 교체 (로컬 데모 모드에는 비밀번호를 남기되 데모임을 명시)
- 실패 사유를 보여주는 `ErrorToast` — 서버가 거절한 이유가 그대로 올라온다

### 검증 상태

- **로컬 모드**: `tsc` / `lint` / `test`(43) / `build` 통과. 브라우저에서 16개 기능 + 자동 정산(90초) + 승급 연출 + 로그인 안내 + 관리자 게이트 재확인, 런타임 에러 0
- **서버 모드**: Supabase 자격증명이 없어 **실행 검증하지 못했다.** 스키마·RLS·함수는 작성만 된 상태

---

## 남은 작업

### 서버 경로 실제 검증 (자격증명 필요)

README 의 셋업 절차대로 프로젝트를 만든 뒤 확인해야 할 것들:

- 마이그레이션이 순서대로 적용되는지
- 가입 → 프로필 자동 생성 → 배팅 → 정산 → 포인트 지급이 도는지
- RLS 가 실제로 막는지 (다른 계정 포인트 수정 시도, 남의 배팅 조회 시도)
- pg_cron 이 마감된 예언을 실제로 정산하는지
- 실시간 구독으로 다른 사람 배팅이 티커에 흐르는지

### 그 밖에

- **서버 정산의 정답이 무작위다.** `settle_due_oracles()` 가 선택률 가중 랜덤으로 고른다. 실서비스에서는 이 자리가 관리자 승인 큐가 되어야 한다.
- 관리자 대시보드 상단 통계(총 사용자, 유통 포인트 등)가 아직 `ADMIN_STATS` 목데이터다. 실제 집계 쿼리로 교체 필요.
- DB 타입을 `npm run types:supabase` 로 생성해 붙이면 스키마와 코드가 어긋날 때 잡힌다. 지금은 Row 인터페이스로 매핑 지점만 고정한 상태.

---

## 기술 사항

- **런타임**: Next.js 16 (App Router) + React 19. 모든 화면이 `"use client"`
- **상태 관리**: React Context. `OracleContext` / `UserContext` / `GradeContext` / `CommentContext` / `UIContext` 를 `AppProvider` 하나가 제공
- **앱 의존성 최소** 원칙 유지 — 런타임 의존성은 `next`, `react`, `lucide-react`, `clsx` 뿐이고 차트·애니메이션은 CSS/Canvas 로 직접 구현. 테스트도 Node 내장 러너를 써서 의존성을 늘리지 않았다
- **배당 모델**: 선택률의 역수에 하우스 엣지 10% 적용 (`lib/betting.ts`). 선택률 합이 항상 100 이 되도록 최대잔여법으로 배분
- **정산**: `lib/settlement.ts` 의 순수 함수. 배당·보너스는 배팅 시점 값으로 고정되고, 이미 정산된 배팅은 다시 지급되지 않는다
- **시간 표시**: 서버/클라이언트 시각 차이로 하이드레이션이 깨지지 않도록 `lib/useNow.ts` 를 반드시 거친다. 렌더 중 `Date.now()` 직접 호출 금지 (린트가 막는다)

## 개발

```bash
npm run dev     # 개발 서버
npm test        # 유닛 테스트 43종 (Node 내장 러너)
npm run lint    # ESLint flat config
npm run build   # 프로덕션 빌드
```
